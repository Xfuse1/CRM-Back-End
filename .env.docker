# Environment file for Docker Compose (backend)
# Copy sensitive values from your existing .env or set appropriate dev values

PORT=3001
CLIENT_ORIGIN=http://localhost:3000
FRONTEND_URL=http://localhost:3000
JWT_SECRET=change_me_in_production

# For local dev you can point to local Postgres (docker-compose db service)
# Replace user/pass/db if different
DATABASE_URL=postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-postgres}
DIRECT_URL=${DATABASE_URL}

# Supabase placeholders (optional if using Supabase remote services)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# Gemini/AI key (optional)
GEMINI_API_KEY=

# Demo owner (optional)
DEMO_OWNER_ID=
